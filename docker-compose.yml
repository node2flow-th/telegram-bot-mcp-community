services:
  telegram-bot-mcp-community:
    build: .
    container_name: telegram-bot-mcp-community
    restart: unless-stopped
    ports:
      - "127.0.0.1:3022:3000"
    mem_limit: 512m
    mem_reservation: 256m
